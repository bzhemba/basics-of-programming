#include <stdio.h>
char *st[];
int main() {
    int n;
    scanf("%s", &st); //считываем строку
    scanf("%d", &n); //считываем целое число
    printf("%s\n", st); //вывод строки
    printf("%d", n); //вывод числа
    return 0;
}
